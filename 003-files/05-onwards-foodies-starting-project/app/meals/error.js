'use client';

export default function Error(){
    return(
        <header className="error">
            <h1>An error has occured!</h1>
            <p>there was problem with fetching the data</p>
        </header>
    );
}